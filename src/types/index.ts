export interface Prototype {
    title: string;
    description: string;
    imageUrl: string;
    playUrl: string;
    githubUrl: string;
    id?: number;
}

export interface AboutData {
    title: string;
    imageUrl: string;
    imageAlt: string;
    content: string;
}
